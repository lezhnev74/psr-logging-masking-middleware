# Copy to .env: `cp .env.dist .env`
# Match these to your host user so files created in the container are owned by you.
# Find your values with: `id -u` (user) and `id -g` (group).
DOCKER_USER_ID=1000
DOCKER_GROUP_ID=1000
